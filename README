RUaCow team presents:
    '"Talangor"'

Instructions:
	Bin folder contains binaries after compilation.
	Makefile and also compiled object files are stored in Build folder.
	Source files are in the Source folder.
